export interface ArticleInput {
  title: string
  description?: string
  images? : Array<any>
}

export interface Article {
  id: string
  title: string
  description?: string
  images? : Array<any>
}